name: passworthy_flags
description: Feature flagging and remote config service for Passworthy.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: 3.24.3

dependencies:
  flagsmith: ^6.0.0
  flutter:
    sdk: flutter
  in_memory_cache:
    path: ../in_memory_cache
  meta: ^1.15.0

dev_dependencies:
  fake_async: ^1.3.1
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  test: ^1.24.8
